# Keep these dimensions aligned with the model checkpoint.
max_action_dim: 24
max_state_dim: 24
max_views: 3

data_groups:
  # Example simulation dataset. Paths are resolved from the directory where
  # you run train.py, typically Evo_1/.
  metaworld_sawyer:
    Evo1_MetaWorld:
      path: ../Evo1_training_dataset/Evo1_MetaWorld_Dataset
      view_map:
        image_1: observation.images.image
